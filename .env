# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-change-this-key-in-production-use-strong-random-key
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=events_db
DB_USER=events_user
DB_PASSWORD=events_password
DB_HOST=localhost
DB_PORT=5432

# JWT Settings (in minutes/days)
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email Settings
# For development, use console backend (OTP will print in terminal)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# For production, use SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-specific-password
# DEFAULT_FROM_EMAIL=noreply@eventsplatform.com

# OTP Settings
OTP_EXPIRY_MINUTES=5
OTP_MAX_ATTEMPTS=3

# Celery Settings (for scheduled emails - bonus feature)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# CORS Settings (if you add a frontend)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
